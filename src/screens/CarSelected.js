import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CarSelected = () => {
  return (
    <View>
      <Text>CarSelected</Text>
    </View>
  );
};

export default CarSelected;

const styles = StyleSheet.create({});
