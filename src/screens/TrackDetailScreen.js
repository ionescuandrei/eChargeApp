import React from 'react';
import { Text, StyleSheet } from 'react-native';

const TrackDetailScreen = () => {
  return <Text style={styles.text}>TrackScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default TrackDetailScreen;
